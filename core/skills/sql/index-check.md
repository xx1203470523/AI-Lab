# 索引检查技能

## 检查清单
- WHERE 条件列是否有索引
- JOIN 关联列是否有索引
- ORDER BY 列是否有索引
- 复合索引列顺序是否合理（等值 → 范围）

## 常见问题
- 时间范围查询没有索引 → 全表扫描
- 状态字段区分度低 → 考虑过滤索引或复合索引
- OR 条件导致索引失效 → 拆成 UNION

## SqlSugar 提示
- 通过 `_context.Db.Queryable<T>().ToSql()` 查看生成的 SQL
- 用数据库工具分析执行计划
