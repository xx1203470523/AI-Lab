# PDA 数据同步检查

## 适用场景
- PDA 提交后 Web 端数据未更新
- PDA 离线操作同步失败
- PDA 与 Web 数据不一致

## 排查流程
1. **检查 PDA 本地状态** — PDA 端是否显示提交成功
2. **查后端日志** — 是否有对应的 API 请求记录
3. **查数据库** — 数据状态是否符合预期
4. **确认幂等** — 重复提交是否被正确拦截

## 常见问题
- PDA 信号弱时提交超时，但后端已处理成功 → 前端应重新查询而非重复提交
- PDA 离线模式下操作需在恢复连接后同步状态
